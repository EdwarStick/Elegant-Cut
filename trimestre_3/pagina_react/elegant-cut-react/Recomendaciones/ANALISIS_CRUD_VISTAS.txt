================================================================================
ANÁLISIS COMPLETO DE VISTAS CRUD - ELEGANT CUT
================================================================================
Fecha: 2025-11-28
Análisis de: Panel de Administración y Sistema CRUD

Este documento detalla QUÉ FALTA en cada vista del CRUD del panel de administración
y proporciona recomendaciones para implementar el CRUD de roles.

================================================================================
1. ESTADO ACTUAL DE LAS VISTAS DEL CRUD
================================================================================

📊 RESUMEN GENERAL:
------------------
✅ ServicesTab.jsx - COMPLETO (100%)
⚠️  DashboardTab.jsx - COMPLETO pero solo lectura (80%)
❌ ClientsTab.jsx - VACÍO (0%)
❌ BarbersTab.jsx - VACÍO (5% - solo estructura básica)
❌ AppointmentsTab.jsx - VACÍO (5% - solo estructura básica)
❌ SettingsTab.jsx - VACÍO (0%)

================================================================================
2. ANÁLISIS DETALLADO POR VISTA
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│ 2.1 SERVICIOS (ServicesTab.jsx) - ✅ COMPLETO                              │
└─────────────────────────────────────────────────────────────────────────────┘

ESTADO: ✅ Completamente funcional

FUNCIONALIDADES IMPLEMENTADAS:
✅ Listar todos los servicios (GET)
✅ Crear nuevo servicio (POST)
✅ Editar servicio existente (PUT)
✅ Eliminar servicio (DELETE)
✅ Modal para crear/editar
✅ Validación de formularios
✅ Mensajes de éxito/error
✅ Loading states
✅ Formateo de moneda
✅ Tabla responsive

ENDPOINTS BACKEND CONECTADOS:
✅ GET    /admin/services
✅ POST   /admin/services
✅ PUT    /admin/services/:id
✅ DELETE /admin/services/:id

LO QUE FALTA: NADA - Esta vista está completa y puede servir como referencia
para implementar las demás vistas.


┌─────────────────────────────────────────────────────────────────────────────┐
│ 2.2 CLIENTES (ClientsTab.jsx) - ❌ VACÍO                                   │
└─────────────────────────────────────────────────────────────────────────────┘

ESTADO: ❌ Solo tiene estructura HTML básica, sin funcionalidad

CÓDIGO ACTUAL:
--------------
const ClientsTab = () => {
  return (
    <div className="tab-content">
      <div className="tab-header">
        <h2>Gestión de Clientes</h2>
      </div>
      {/* Tabla de clientes vendrá aquí */}
    </div>
  );
};

LO QUE FALTA IMPLEMENTAR:
-------------------------

FRONTEND (ClientsTab.jsx):
□ Estado para almacenar lista de clientes
□ useEffect para cargar clientes al montar componente
□ Función loadClients() para obtener datos del backend
□ Tabla para mostrar clientes con columnas:
  - ID
  - Nombre completo
  - Email
  - Teléfono
  - Rol
  - Estado (Activo/Inactivo)
  - Fecha de registro
  - Acciones (Ver, Editar, Desactivar)
□ Modal para ver detalles del cliente
□ Modal para editar cliente
□ Función para cambiar estado (activar/desactivar)
□ Filtros de búsqueda (por nombre, email, rol)
□ Paginación (si hay muchos clientes)
□ Loading states
□ Mensajes de éxito/error

BACKEND (auth_fixed.js):
□ GET    /admin/clients - Obtener todos los clientes
□ GET    /admin/clients/:id - Obtener un cliente específico
□ PUT    /admin/clients/:id - Actualizar datos del cliente
□ DELETE /admin/clients/:id - Desactivar cliente (cambiar estado a 0)

CONSULTAS SQL NECESARIAS:
--------------------------
-- Obtener todos los clientes
SELECT u.id_usuario, u.username, u.email, u.prim_nombre, u.seg_nombre, 
       u.apellido1, u.apellido2, u.telefono, r.nombre_rol, u.estado, u.created_at
FROM usuarios u
LEFT JOIN rol r ON u.id_rol = r.id_rol
WHERE u.id_rol = 3  -- Solo clientes
ORDER BY u.created_at DESC;

-- Obtener cliente por ID
SELECT u.*, r.nombre_rol, COUNT(res.id_reservas) as total_citas
FROM usuarios u
LEFT JOIN rol r ON u.id_rol = r.id_rol
LEFT JOIN reservas res ON u.id_usuario = res.id_usuario
WHERE u.id_usuario = ?
GROUP BY u.id_usuario;

-- Actualizar cliente
UPDATE usuarios 
SET prim_nombre = ?, seg_nombre = ?, apellido1 = ?, apellido2 = ?, 
    email = ?, telefono = ?, updated_at = CURRENT_TIMESTAMP
WHERE id_usuario = ?;

-- Desactivar cliente
UPDATE usuarios SET estado = 0, updated_at = CURRENT_TIMESTAMP WHERE id_usuario = ?;


┌─────────────────────────────────────────────────────────────────────────────┐
│ 2.3 BARBEROS (BarbersTab.jsx) - ❌ VACÍO                                   │
└─────────────────────────────────────────────────────────────────────────────┘

ESTADO: ❌ Solo tiene estructura HTML básica con botón sin funcionalidad

CÓDIGO ACTUAL:
--------------
const BarbersTab = () => {
  return (
    <div className="tab-content">
      <div className="tab-header">
        <h2>Gestión de Barberos</h2>
        <button className="btn btn-primary">
          <i className="bi bi-person-plus"></i> Agregar Barbero
        </button>
      </div>
      {/* Lista de barberos vendrá aquí */}
    </div>
  );
};

LO QUE FALTA IMPLEMENTAR:
-------------------------

FRONTEND (BarbersTab.jsx):
□ Estado para almacenar lista de barberos
□ useEffect para cargar barberos al montar componente
□ Función loadBarbers() para obtener datos del backend
□ Tabla/Grid de barberos con:
  - Foto de perfil (opcional)
  - Nombre completo
  - Email
  - Teléfono
  - Especialidades (opcional)
  - Estado (Activo/Inactivo)
  - Estadísticas (total de citas atendidas)
  - Acciones (Ver, Editar, Desactivar)
□ Modal para agregar nuevo barbero
□ Modal para editar barbero existente
□ Función para crear barbero (crear usuario con rol barbero)
□ Función para actualizar barbero
□ Función para desactivar barbero
□ Validación de formularios
□ Loading states
□ Mensajes de éxito/error

BACKEND (auth_fixed.js):
□ GET    /admin/barbers - Obtener todos los barberos
□ GET    /admin/barbers/:id - Obtener un barbero específico
□ POST   /admin/barbers - Crear nuevo barbero
□ PUT    /admin/barbers/:id - Actualizar barbero
□ DELETE /admin/barbers/:id - Desactivar barbero

CONSULTAS SQL NECESARIAS:
--------------------------
-- Obtener todos los barberos con estadísticas
SELECT u.id_usuario, u.username, u.email, u.prim_nombre, u.seg_nombre,
       u.apellido1, u.apellido2, u.telefono, u.estado, u.created_at,
       COUNT(DISTINCT res.id_reservas) as total_citas,
       COUNT(DISTINCT CASE WHEN res.fecha = CURDATE() THEN res.id_reservas END) as citas_hoy
FROM usuarios u
LEFT JOIN reservas res ON u.id_usuario = res.id_usuario
WHERE u.id_rol = 2  -- Solo barberos
GROUP BY u.id_usuario
ORDER BY u.created_at DESC;

-- Crear barbero (insertar usuario con rol 2)
INSERT INTO usuarios (username, password_hash, email, prim_nombre, seg_nombre, 
                      apellido1, apellido2, telefono, id_rol, estado)
VALUES (?, ?, ?, ?, ?, ?, ?, ?, 2, 1);

-- Actualizar barbero
UPDATE usuarios 
SET prim_nombre = ?, seg_nombre = ?, apellido1 = ?, apellido2 = ?,
    email = ?, telefono = ?, updated_at = CURRENT_TIMESTAMP
WHERE id_usuario = ? AND id_rol = 2;

-- Desactivar barbero
UPDATE usuarios SET estado = 0, updated_at = CURRENT_TIMESTAMP 
WHERE id_usuario = ? AND id_rol = 2;


┌─────────────────────────────────────────────────────────────────────────────┐
│ 2.4 CITAS (AppointmentsTab.jsx) - ❌ VACÍO                                 │
└─────────────────────────────────────────────────────────────────────────────┘

ESTADO: ❌ Solo tiene estructura HTML básica con botón sin funcionalidad

CÓDIGO ACTUAL:
--------------
const AppointmentsTab = () => {
  return (
    <div className="tab-content">
      <div className="tab-header">
        <h2>Gestión de Citas</h2>
        <button className="btn btn-primary">
          <i className="bi bi-plus-circle"></i> Nueva Cita
        </button>
      </div>
      {/* Tabla de citas vendrá aquí */}
    </div>
  );
};

LO QUE FALTA IMPLEMENTAR:
-------------------------

FRONTEND (AppointmentsTab.jsx):
□ Estado para almacenar lista de citas
□ useEffect para cargar citas al montar componente
□ Función loadAppointments() para obtener datos del backend
□ Tabla de citas con columnas:
  - ID Cita
  - Fecha
  - Hora
  - Cliente (nombre)
  - Barbero (nombre)
  - Servicio
  - Estado (Pendiente, Confirmada, Cancelada, Completada)
  - Precio
  - Acciones (Ver, Cambiar Estado, Cancelar)
□ Filtros por:
  - Fecha (hoy, esta semana, este mes, rango personalizado)
  - Estado
  - Barbero
□ Modal para ver detalles de la cita
□ Modal para crear nueva cita (admin puede agendar por teléfono)
□ Función para cambiar estado de cita
□ Función para cancelar cita
□ Vista de calendario (opcional pero recomendado)
□ Loading states
□ Mensajes de éxito/error
□ Badges de colores según estado

BACKEND (auth_fixed.js):
✅ GET    /admin/appointments - YA EXISTE (línea 665)
✅ PUT    /admin/appointments/:id - YA EXISTE (línea 679)
□ POST   /admin/appointments - Crear cita desde admin
□ GET    /admin/appointments/today - Citas del día
□ GET    /admin/appointments/upcoming - Próximas citas

NOTA: El backend YA TIENE endpoints básicos, pero el frontend no los está usando.

CONSULTAS SQL NECESARIAS:
--------------------------
-- Obtener todas las citas con detalles (YA EXISTE en models/Appointment.js)
SELECT r.id_reservas, r.fecha, r.observaciones,
       CONCAT(u.prim_nombre, ' ', u.apellido1) as cliente,
       u.telefono,
       s.nombre as servicio, s.precio,
       h.hora_inicio,
       ec.nombre_estado as estado
FROM reservas r
JOIN usuarios u ON r.id_usuario = u.id_usuario
JOIN detalle_cita_servicio dcs ON r.id_reservas = dcs.id_reservas
JOIN servicios s ON dcs.id_servicio = s.id_servicio
JOIN horarios h ON r.id_horarios = h.id_horarios
JOIN estado_cita ec ON r.id_estado_cita = ec.id_estado_cita
ORDER BY r.fecha DESC, h.hora_inicio DESC;

-- Cambiar estado de cita (YA EXISTE en models/Appointment.js)
UPDATE reservas SET id_estado_cita = ?, updated_at = CURRENT_TIMESTAMP 
WHERE id_reservas = ?;

-- Citas del día
SELECT * FROM reservas WHERE fecha = CURDATE();

-- Próximas citas (próximos 7 días)
SELECT * FROM reservas 
WHERE fecha BETWEEN CURDATE() AND DATE_ADD(CURDATE(), INTERVAL 7 DAY)
ORDER BY fecha ASC, id_horarios ASC;


┌─────────────────────────────────────────────────────────────────────────────┐
│ 2.5 CONFIGURACIÓN (SettingsTab.jsx) - ❌ VACÍO                             │
└─────────────────────────────────────────────────────────────────────────────┘

ESTADO: ❌ Solo tiene estructura HTML básica

CÓDIGO ACTUAL:
--------------
const SettingsTab = () => {
  return (
    <div className="tab-content">
      <div className="tab-header">
        <h2>Configuración del Sistema</h2>
      </div>
      {/* Formulario de configuración vendrá aquí */}
    </div>
  );
};

LO QUE FALTA IMPLEMENTAR:
-------------------------

FRONTEND (SettingsTab.jsx):
□ Secciones de configuración:
  
  1. CONFIGURACIÓN GENERAL:
     □ Nombre de la barbería
     □ Dirección
     □ Teléfono de contacto
     □ Email de contacto
     □ Horario de atención
     □ Logo de la barbería
  
  2. CONFIGURACIÓN DE HORARIOS:
     □ Gestión de bloques de horarios disponibles
     □ Agregar/editar/eliminar horarios
     □ Días laborales
  
  3. CONFIGURACIÓN DE NOTIFICACIONES:
     □ Email para notificaciones
     □ Activar/desactivar notificaciones por email
     □ Plantillas de email
  
  4. GESTIÓN DE ROLES (ver sección 3)
  
  5. CONFIGURACIÓN DE PAGOS:
     □ Métodos de pago aceptados
     □ Configuración de pasarela de pago (futuro)

□ Formularios para cada sección
□ Función para guardar configuraciones
□ Loading states
□ Mensajes de éxito/error

BACKEND (auth_fixed.js):
□ GET    /admin/settings - Obtener configuraciones
□ PUT    /admin/settings - Actualizar configuraciones
□ GET    /admin/horarios - Obtener horarios
□ POST   /admin/horarios - Crear horario
□ DELETE /admin/horarios/:id - Eliminar horario

TABLA SQL NECESARIA:
--------------------
-- Crear tabla de configuraciones
CREATE TABLE IF NOT EXISTS configuraciones (
    id_config INT AUTO_INCREMENT PRIMARY KEY,
    clave VARCHAR(50) NOT NULL UNIQUE,
    valor TEXT,
    descripcion VARCHAR(255),
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- Insertar configuraciones por defecto
INSERT INTO configuraciones (clave, valor, descripcion) VALUES
('nombre_barberia', 'Elegant Cut', 'Nombre de la barbería'),
('direccion', '', 'Dirección física'),
('telefono', '', 'Teléfono de contacto'),
('email', '', 'Email de contacto'),
('horario_apertura', '08:00', 'Hora de apertura'),
('horario_cierre', '18:00', 'Hora de cierre');


┌─────────────────────────────────────────────────────────────────────────────┐
│ 2.6 DASHBOARD (DashboardTab.jsx) - ⚠️ COMPLETO (solo lectura)              │
└─────────────────────────────────────────────────────────────────────────────┘

ESTADO: ✅ Funcional pero solo muestra datos (sin acciones CRUD)

FUNCIONALIDADES IMPLEMENTADAS:
✅ Mostrar estadísticas generales
✅ Actividad reciente
✅ Próximas citas
✅ Loading states
✅ Formateo de datos

LO QUE PODRÍA MEJORARSE:
□ Gráficos de estadísticas (Chart.js o Recharts)
□ Filtros por fecha
□ Exportar reportes
□ Acciones rápidas desde el dashboard


================================================================================
3. RECOMENDACIÓN: IMPLEMENTACIÓN DEL CRUD DE ROLES
================================================================================

📋 CONTEXTO:
------------
Actualmente tienes 3 roles en tu sistema:
1. Admin (id_rol = 1)
2. Barbero (id_rol = 2)
3. Cliente (id_rol = 3)

Estos roles están en la tabla 'rol' y se relacionan con usuarios a través de
la columna 'id_rol' en la tabla 'usuarios'.

🎯 OPCIÓN RECOMENDADA: Implementar el CRUD de Roles en SettingsTab
-------------------------------------------------------------------

RAZÓN: El CRUD de roles es una funcionalidad administrativa avanzada que no
se usa frecuentemente. Es más apropiado tenerlo en la sección de Configuración
que en una pestaña separada.

IMPLEMENTACIÓN SUGERIDA:
------------------------

1. UBICACIÓN: 
   SettingsTab.jsx → Sección "Gestión de Roles y Permisos"

2. ACCESO:
   Solo usuarios con rol "admin" (id_rol = 1) deben poder:
   - Ver todos los roles
   - Crear nuevos roles
   - Editar roles existentes
   - NO ELIMINAR roles base (admin, barbero, cliente)
   - Eliminar solo roles personalizados

3. FUNCIONALIDADES A IMPLEMENTAR:

   FRONTEND (dentro de SettingsTab.jsx):
   □ Sección "Gestión de Roles"
   □ Tabla de roles existentes con:
     - ID del rol
     - Nombre del rol
     - Cantidad de usuarios con ese rol
     - Acciones (Editar, Eliminar*)
   □ Botón "Agregar Nuevo Rol"
   □ Modal para crear/editar rol
   □ Validación: no permitir eliminar roles con usuarios asignados
   □ Validación: no permitir eliminar roles base (admin, barbero, cliente)
   
   BACKEND (auth_fixed.js):
   □ GET    /admin/roles - Obtener todos los roles
   □ POST   /admin/roles - Crear nuevo rol
   □ PUT    /admin/roles/:id - Actualizar rol
   □ DELETE /admin/roles/:id - Eliminar rol (con validaciones)

4. CONSULTAS SQL:

-- Obtener todos los roles con cantidad de usuarios
SELECT r.id_rol, r.nombre_rol, COUNT(u.id_usuario) as total_usuarios
FROM rol r
LEFT JOIN usuarios u ON r.id_rol = u.id_rol
GROUP BY r.id_rol
ORDER BY r.id_rol ASC;

-- Crear nuevo rol
INSERT INTO rol (nombre_rol) VALUES (?);

-- Actualizar rol
UPDATE rol SET nombre_rol = ? WHERE id_rol = ?;

-- Eliminar rol (solo si no tiene usuarios asignados)
DELETE FROM rol 
WHERE id_rol = ? 
AND id_rol NOT IN (1, 2, 3)  -- Proteger roles base
AND NOT EXISTS (SELECT 1 FROM usuarios WHERE id_rol = ?);

5. VALIDACIONES IMPORTANTES:

   ✅ No permitir nombres de rol duplicados
   ✅ No permitir eliminar roles con usuarios asignados
   ✅ No permitir eliminar roles base (admin, barbero, cliente)
   ✅ No permitir editar el nombre de roles base
   ✅ Validar que el usuario que hace la acción sea admin

6. PERMISOS POR ROL (Recomendación para el futuro):

   Si quieres un sistema más robusto, puedes crear una tabla de permisos:

   CREATE TABLE IF NOT EXISTS permisos (
       id_permiso INT AUTO_INCREMENT PRIMARY KEY,
       nombre_permiso VARCHAR(50) NOT NULL UNIQUE,
       descripcion VARCHAR(255)
   );

   CREATE TABLE IF NOT EXISTS rol_permisos (
       id_rol INT,
       id_permiso INT,
       PRIMARY KEY (id_rol, id_permiso),
       FOREIGN KEY (id_rol) REFERENCES rol(id_rol),
       FOREIGN KEY (id_permiso) REFERENCES permisos(id_permiso)
   );

   Ejemplos de permisos:
   - ver_dashboard
   - gestionar_servicios
   - gestionar_barberos
   - gestionar_clientes
   - gestionar_citas
   - gestionar_roles
   - ver_reportes

7. CONTROL DE ACCESO EN EL FRONTEND:

   En cada Tab, verificar el rol del usuario:

   const user = JSON.parse(localStorage.getItem('user'));
   
   // Solo admin puede ver ciertas tabs
   if (user.role !== 'admin') {
     return <div>No tienes permisos para ver esta sección</div>;
   }

8. MIDDLEWARE DE AUTENTICACIÓN EN BACKEND:

   Crear un middleware para verificar el token y el rol:

   const verificarAdmin = (req, res, next) => {
     const token = req.headers.authorization?.split(' ')[1];
     
     if (!token) {
       return res.status(401).json({ error: 'No autorizado' });
     }
     
     try {
       const decoded = jwt.verify(token, JWT_SECRET);
       
       if (decoded.role !== 'admin') {
         return res.status(403).json({ error: 'Acceso denegado' });
       }
       
       req.user = decoded;
       next();
     } catch (error) {
       return res.status(401).json({ error: 'Token inválido' });
     }
   };

   // Usar en las rutas protegidas
   if (req.url === '/admin/roles' && req.method === 'POST') {
     verificarAdmin(req, res, () => {
       // Lógica para crear rol
     });
   }


================================================================================
4. PRIORIDADES DE IMPLEMENTACIÓN RECOMENDADAS
================================================================================

Basándome en la importancia para el negocio, te recomiendo implementar en este
orden:

🥇 PRIORIDAD ALTA (Implementar primero):
-----------------------------------------
1. AppointmentsTab.jsx - Gestión de Citas
   Razón: Es el core del negocio. Los admins necesitan ver y gestionar citas.

2. ClientsTab.jsx - Gestión de Clientes
   Razón: Importante para ver el historial y datos de clientes.

3. BarbersTab.jsx - Gestión de Barberos
   Razón: Necesario para agregar/editar barberos del equipo.

🥈 PRIORIDAD MEDIA (Implementar después):
------------------------------------------
4. SettingsTab.jsx - Configuración General
   Razón: Útil para personalizar el sistema.

5. SettingsTab.jsx - CRUD de Roles
   Razón: Funcionalidad administrativa avanzada.

🥉 PRIORIDAD BAJA (Mejoras futuras):
-------------------------------------
6. Dashboard - Gráficos y reportes
   Razón: El dashboard actual ya funciona, esto sería un plus.

7. Sistema de permisos granular
   Razón: Solo si necesitas control de acceso muy específico.


================================================================================
5. ESTRUCTURA DE ARCHIVOS RECOMENDADA
================================================================================

Para mantener el código organizado, te recomiendo esta estructura:

src/
├── components/
│   ├── AdminHeader.jsx ✅
│   ├── Sidebar.jsx ✅
│   ├── DashboardTab.jsx ✅
│   ├── ServicesTab.jsx ✅
│   ├── BarbersTab.jsx ❌ (implementar)
│   ├── AppointmentsTab.jsx ❌ (implementar)
│   ├── ClientsTab.jsx ❌ (implementar)
│   ├── SettingsTab.jsx ❌ (implementar)
│   │
│   └── shared/ (crear esta carpeta)
│       ├── Modal.jsx (componente reutilizable)
│       ├── Table.jsx (componente reutilizable)
│       ├── LoadingSpinner.jsx
│       └── AlertMessage.jsx
│
├── hooks/
│   ├── useAuth.js ✅
│   └── useCrud.js (crear - hook reutilizable para CRUD)
│
└── utils/
    ├── authClient.js ✅
    ├── api.js (crear - funciones para llamadas al backend)
    └── formatters.js (crear - funciones de formateo)

backend/
├── auth_fixed.js ✅
├── models/
│   ├── Dashboard.js ✅
│   ├── Service.js ✅
│   ├── Appointment.js ✅
│   ├── Client.js ❌ (crear)
│   ├── Barber.js ❌ (crear)
│   └── Role.js ❌ (crear)
│
└── middleware/
    ├── auth.js ❌ (crear - middleware de autenticación)
    └── roleCheck.js ❌ (crear - verificar roles)


================================================================================
6. EJEMPLO DE CÓDIGO PARA IMPLEMENTAR ClientsTab.jsx
================================================================================

Te dejo un ejemplo completo de cómo implementar ClientsTab.jsx siguiendo el
patrón de ServicesTab.jsx:

```javascript
import React, { useState, useEffect } from 'react';

const ClientsTab = () => {
  const [clients, setClients] = useState([]);
  const [loading, setLoading] = useState(false);
  const [showModal, setShowModal] = useState(false);
  const [selectedClient, setSelectedClient] = useState(null);
  const [message, setMessage] = useState({ text: '', type: '' });

  useEffect(() => {
    loadClients();
  }, []);

  const loadClients = async () => {
    try {
      setLoading(true);
      const response = await fetch('http://localhost:3001/admin/clients');
      const data = await response.json();
      
      if (data.success) {
        setClients(data.data);
      } else {
        showMessage('Error cargando clientes', 'error');
      }
    } catch (error) {
      console.error('Error:', error);
      showMessage('Error de conexión', 'error');
    } finally {
      setLoading(false);
    }
  };

  const showMessage = (text, type) => {
    setMessage({ text, type });
    setTimeout(() => setMessage({ text: '', type: '' }), 4000);
  };

  const handleViewClient = (client) => {
    setSelectedClient(client);
    setShowModal(true);
  };

  const handleToggleStatus = async (clientId, currentStatus) => {
    if (!window.confirm('¿Cambiar el estado de este cliente?')) return;

    try {
      setLoading(true);
      const response = await fetch(`http://localhost:3001/admin/clients/${clientId}`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ estado: currentStatus === 1 ? 0 : 1 })
      });

      const data = await response.json();
      
      if (data.success) {
        showMessage('Estado actualizado', 'success');
        loadClients();
      }
    } catch (error) {
      showMessage('Error actualizando estado', 'error');
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="tab-content">
      <div className="tab-header">
        <h2>Gestión de Clientes</h2>
        <button className="btn btn-primary" onClick={loadClients}>
          <i className="bi bi-arrow-clockwise"></i> Actualizar
        </button>
      </div>

      {message.text && (
        <div className={`alert alert-${message.type}`}>
          {message.text}
        </div>
      )}

      <div className="table-container">
        {loading && clients.length === 0 ? (
          <div className="loading-container">
            <div className="spinner-border text-primary"></div>
            <p>Cargando clientes...</p>
          </div>
        ) : clients.length === 0 ? (
          <div className="no-data">
            <i className="bi bi-people"></i>
            <h4>No hay clientes registrados</h4>
          </div>
        ) : (
          <table className="crud-table">
            <thead>
              <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Email</th>
                <th>Teléfono</th>
                <th>Estado</th>
                <th>Registro</th>
                <th>Acciones</th>
              </tr>
            </thead>
            <tbody>
              {clients.map((client) => (
                <tr key={client.id_usuario}>
                  <td>#{client.id_usuario}</td>
                  <td>
                    <strong>
                      {client.prim_nombre} {client.apellido1}
                    </strong>
                  </td>
                  <td>{client.email}</td>
                  <td>{client.telefono}</td>
                  <td>
                    <span className={`badge ${client.estado === 1 ? 'bg-success' : 'bg-danger'}`}>
                      {client.estado === 1 ? 'Activo' : 'Inactivo'}
                    </span>
                  </td>
                  <td>
                    {new Date(client.created_at).toLocaleDateString('es-ES')}
                  </td>
                  <td>
                    <div className="action-buttons">
                      <button 
                        className="btn btn-sm btn-outline-primary"
                        onClick={() => handleViewClient(client)}
                      >
                        <i className="bi bi-eye"></i>
                      </button>
                      <button 
                        className="btn btn-sm btn-outline-warning"
                        onClick={() => handleToggleStatus(client.id_usuario, client.estado)}
                      >
                        <i className="bi bi-toggle-on"></i>
                      </button>
                    </div>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        )}
      </div>

      {/* Modal para ver detalles */}
      {showModal && selectedClient && (
        <div className="modal-overlay">
          <div className="modal-content">
            <div className="modal-header">
              <h5>Detalles del Cliente</h5>
              <button onClick={() => setShowModal(false)}>×</button>
            </div>
            <div className="modal-body">
              <p><strong>Nombre:</strong> {selectedClient.prim_nombre} {selectedClient.apellido1}</p>
              <p><strong>Email:</strong> {selectedClient.email}</p>
              <p><strong>Teléfono:</strong> {selectedClient.telefono}</p>
              <p><strong>Estado:</strong> {selectedClient.estado === 1 ? 'Activo' : 'Inactivo'}</p>
            </div>
          </div>
        </div>
      )}
    </div>
  );
};

export default ClientsTab;
```

ENDPOINT BACKEND NECESARIO:

```javascript
// En auth_fixed.js, agregar:

// CLIENTES - GET ALL
if (req.url === '/admin/clients' && req.method === 'GET') {
    try {
        const [clients] = await pool.execute(
            `SELECT u.id_usuario, u.username, u.email, u.prim_nombre, u.seg_nombre,
                    u.apellido1, u.apellido2, u.telefono, u.estado, u.created_at
             FROM usuarios u
             WHERE u.id_rol = 3
             ORDER BY u.created_at DESC`
        );
        
        res.writeHead(200, { 'Content-Type': 'application/json' });
        res.end(JSON.stringify({ success: true, data: clients }));
    } catch (error) {
        console.log('💥 Error obteniendo clientes:', error);
        res.writeHead(500, { 'Content-Type': 'application/json' });
        res.end(JSON.stringify({ success: false, error: 'Error obteniendo clientes' }));
    }
    return;
}

// CLIENTES - UPDATE STATUS
if (req.url.startsWith('/admin/clients/') && req.method === 'PUT') {
    let body = '';
    req.on('data', chunk => body += chunk.toString());
    
    req.on('end', async () => {
        try {
            const id = req.url.split('/')[3];
            const { estado } = JSON.parse(body);
            
            const [result] = await pool.execute(
                'UPDATE usuarios SET estado = ?, updated_at = CURRENT_TIMESTAMP WHERE id_usuario = ? AND id_rol = 3',
                [estado, id]
            );
            
            if (result.affectedRows > 0) {
                res.writeHead(200, { 'Content-Type': 'application/json' });
                res.end(JSON.stringify({ success: true, message: 'Cliente actualizado' }));
            } else {
                res.writeHead(404, { 'Content-Type': 'application/json' });
                res.end(JSON.stringify({ success: false, error: 'Cliente no encontrado' }));
            }
        } catch (error) {
            console.log('💥 Error actualizando cliente:', error);
            res.writeHead(500, { 'Content-Type': 'application/json' });
            res.end(JSON.stringify({ success: false, error: 'Error actualizando cliente' }));
        }
    });
    return;
}
```


================================================================================
7. RESUMEN Y PRÓXIMOS PASOS
================================================================================

RESUMEN DE LO QUE FALTA:
-------------------------
✅ ServicesTab - COMPLETO
✅ DashboardTab - COMPLETO (solo lectura)
❌ ClientsTab - TODO por implementar
❌ BarbersTab - TODO por implementar
❌ AppointmentsTab - TODO por implementar (backend existe, falta frontend)
❌ SettingsTab - TODO por implementar
❌ CRUD de Roles - TODO por implementar (recomendado en SettingsTab)

PRÓXIMOS PASOS RECOMENDADOS:
-----------------------------
1. Implementar AppointmentsTab.jsx (PRIORIDAD ALTA)
2. Implementar ClientsTab.jsx (PRIORIDAD ALTA)
3. Implementar BarbersTab.jsx (PRIORIDAD ALTA)
4. Implementar SettingsTab.jsx con sección de Roles (PRIORIDAD MEDIA)
5. Agregar middleware de autenticación en backend (PRIORIDAD MEDIA)
6. Implementar sistema de permisos granular (OPCIONAL)

RECOMENDACIÓN FINAL SOBRE CRUD DE ROLES:
-----------------------------------------
✅ IMPLEMENTAR en SettingsTab.jsx como una sección
✅ ACCESO solo para usuarios con rol 'admin'
✅ PROTEGER roles base (admin, barbero, cliente) de eliminación
✅ VALIDAR que no se eliminen roles con usuarios asignados

Esto te dará un sistema robusto y profesional sin complicar demasiado la
interfaz del panel de administración.

================================================================================
FIN DEL ANÁLISIS
================================================================================

¿Necesitas ayuda para implementar alguna de estas vistas? Puedo ayudarte a
crear el código completo para cualquiera de ellas siguiendo el patrón de
ServicesTab.jsx que ya tienes funcionando.
